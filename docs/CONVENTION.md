# Convention

## Test

- 테스트명은 한글로 작성한다.
- 테스트명에는 입력과 기대 결과가 함께 드러나야 한다.
- 함수나 동작의 입력값은 테스트명에 그대로 표시한다.
- 기대 결과는 "반환한다", "저장한다", "표시한다"처럼 관찰 가능한 결과로 표현한다.

예시:

- `add(1, 1)은 2를 반환한다`
- `upper("aaa")는 "AAA"를 반환한다`
